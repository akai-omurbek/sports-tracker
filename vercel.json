{
  "version": 2,
  "buildCommand": "npm --prefix frontend install && CI=false npm --prefix frontend run build",
  "outputDirectory": "frontend/build",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index.js" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
